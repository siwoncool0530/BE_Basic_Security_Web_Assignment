spring:
  application:
    name: assignment

  datasource:
    url: jdbc:mysql://localhost:3306/posts?serverTimezone=Asia/Seoul
    username: test_user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect